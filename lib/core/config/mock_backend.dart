/// Global toggle for running the app without backend/database.
///
/// Keep this `true` while backend is unavailable, then flip to `false`
/// to restore real API calls.
const kUseMockBackend = true;

const kMockLoginEmail = 'test@voyanz.com';
const kMockLoginPassword = 'Test123!';
